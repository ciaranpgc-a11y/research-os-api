# Storybook Archive

Archived stories, not part of approval surface.

These files are intentionally excluded from active Storybook discovery.
Restore selectively if needed, but approvals must remain single-source in `Design System / Approvals`.
