import { Meta } from '@storybook/blocks'

import { ApprovalsContent } from './ApprovalsContent'

<Meta title="Design System / Approvals" parameters={{ layout: 'fullscreen' }} />

<ApprovalsContent />
